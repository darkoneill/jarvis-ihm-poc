# Jarvis IHM - Configuration Docker
# Copier ce fichier en docker.env et modifier les valeurs

# ============================================
# Base de données MySQL
# ============================================
MYSQL_ROOT_PASSWORD=rootpassword
DATABASE_URL=mysql://jarvis:jarvis2024@mysql:3306/jarvis_ihm

# ============================================
# Authentification
# ============================================
# Activer l'authentification locale (sans Manus OAuth)
LOCAL_AUTH_ENABLED=true

# Clé secrète JWT (CHANGER EN PRODUCTION!)
JWT_SECRET=jarvis-secret-key-change-in-production

# ============================================
# Configuration LLM (optionnel)
# ============================================
# N2 Supervisor (LLM local sur DGX Spark)
N2_ENABLED=false
N2_API_URL=http://host.docker.internal:8000

# Ollama (LLM local)
OLLAMA_URL=http://host.docker.internal:11434

# Forge API (cloud)
FORGE_API_KEY=

# OpenAI (cloud)
OPENAI_API_KEY=

# Anthropic (cloud)
ANTHROPIC_API_KEY=

# ============================================
# Monitoring (optionnel)
# ============================================
# Prometheus
PROMETHEUS_ENABLED=false
PROMETHEUS_URL=http://host.docker.internal:9090

# Redis est configuré automatiquement via docker-compose
# REDIS_URL=redis://redis:6379
